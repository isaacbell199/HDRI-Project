# (C3) Fix: Pin Rust toolchain version for reproducible builds
# OnceLock (std::sync::OnceLock) requires Rust 1.80+
# Using 1.85.0 as the minimum guaranteed stable version
[toolchain]
channel = "1.85.0"
targets = ["x86_64-pc-windows-msvc"]
